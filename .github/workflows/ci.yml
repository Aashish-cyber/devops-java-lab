name: Java DevOps CI-CD

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/aashish-cyber/devops-java-lab

jobs:

  # ==========================================================
  # CI
  # ==========================================================
  build:
    name: Build, Test & Push Image
    runs-on: [self-hosted, linux, x64]

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Check Java Version
        run: java --version

      - name: Check Maven Version
        run: mvn --version

      - name: Build and Test
        run: mvn clean package

      - name: Verify WAR Artifact
        run: ls -lh target/*.war

      - name: Generate Image Tag
        run: |
          echo "IMAGE_TAG=${GITHUB_SHA}" >> "$GITHUB_ENV"
          echo "Docker Image Tag: ${GITHUB_SHA}"

      - name: Build Docker Image
        run: |
          docker build \
            -t "$IMAGE_NAME:$IMAGE_TAG" \
            .

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Image to GHCR
        run: |
          docker push "$IMAGE_NAME:$IMAGE_TAG"


  # ==========================================================
  # BLUE/GREEN DEPLOYMENT
  # ==========================================================
  deploy:
    name: Blue/Green Deployment
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build

    runs-on: [self-hosted, linux, x64]

    env:
      BLUE_CONTAINER: devops-java-deploy
      GREEN_CONTAINER: devops-java-green
      NGINX_CONTAINER: devops-nginx

      DOCKER_NETWORK: blue-green-net

      BLUE_PORT: 8082
      GREEN_PORT: 8083
      PRODUCTION_PORT: 8080

    steps:

      # ======================================================
      # 1. Checkout
      # ======================================================
      - name: Checkout Code
        uses: actions/checkout@v4


      # ======================================================
      # 2. Generate Image Tag
      # ======================================================
      - name: Generate Image Tag
        run: |
          echo "IMAGE_TAG=${GITHUB_SHA}" >> "$GITHUB_ENV"

          echo "====================================="
          echo "IMAGE"
          echo "====================================="
          echo "$IMAGE_NAME:$GITHUB_SHA"


      # ======================================================
      # 3. Login GHCR
      # ======================================================
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}


      # ======================================================
      # 4. Pull New Image
      # ======================================================
      - name: Pull New Image
        run: |
          docker pull "$IMAGE_NAME:$IMAGE_TAG"


      # ======================================================
      # 5. Verify BLUE
      # ======================================================
      - name: Verify Blue Environment
        run: |

          echo "====================================="
          echo "VERIFYING BLUE"
          echo "====================================="

          if ! docker inspect "$BLUE_CONTAINER" >/dev/null 2>&1; then
            echo "ERROR: Blue container does not exist."
            exit 1
          fi

          if ! docker ps \
            --filter "name=$BLUE_CONTAINER" \
            --filter "status=running" \
            -q | grep -q .; then

            echo "ERROR: Blue container is not running."
            exit 1
          fi

          echo "Blue container is running."

          echo ""
          echo "Blue image:"
          docker inspect "$BLUE_CONTAINER" \
            --format '{{.Config.Image}}'

          echo ""
          echo "Testing Blue directly..."

          curl -fsS \
            "http://localhost:$BLUE_PORT/devops-lab/" \
            > /tmp/blue-health.html

          cat /tmp/blue-health.html


      # ======================================================
      # 6. Save BLUE Nginx Configuration
      # ======================================================
      - name: Save Blue Nginx Configuration
        run: |

          echo "====================================="
          echo "SAVING BLUE NGINX CONFIGURATION"
          echo "====================================="

          cp nginx/nginx.conf /tmp/nginx-blue.conf

          echo ""
          echo "Blue configuration:"
          cat /tmp/nginx-blue.conf


      # ======================================================
      # 7. Remove Previous GREEN
      # ======================================================
      - name: Remove Previous Green
        run: |

          echo "====================================="
          echo "REMOVING OLD GREEN"
          echo "====================================="

          docker rm -f "$GREEN_CONTAINER" 2>/dev/null || true


      # ======================================================
      # 8. Deploy NEW VERSION to GREEN
      # ======================================================
      - name: Deploy New Version to Green
        run: |

          echo "====================================="
          echo "DEPLOYING NEW VERSION TO GREEN"
          echo "====================================="

          docker run -d \
            --name "$GREEN_CONTAINER" \
            --network "$DOCKER_NETWORK" \
            -p "$GREEN_PORT:8080" \
            "$IMAGE_NAME:$IMAGE_TAG"

          echo ""
          echo "Green container:"
          docker ps --filter "name=$GREEN_CONTAINER"

          echo ""
          echo "Green image:"
          docker inspect "$GREEN_CONTAINER" \
            --format '{{.Config.Image}}'


      # ======================================================
      # 9. HEALTH CHECK GREEN
      # ======================================================
      - name: Health Check Green
        run: |

          echo "====================================="
          echo "HEALTH CHECK - GREEN"
          echo "====================================="

          for i in {1..12}; do

            echo "Health check attempt $i/12"

            if curl -fsS \
              "http://localhost:$GREEN_PORT/devops-lab/" \
              > /tmp/green-health.html; then

              echo ""
              echo "GREEN IS HEALTHY!"
              echo ""

              cat /tmp/green-health.html

              exit 0
            fi

            sleep 5

          done

          echo ""
          echo "GREEN FAILED HEALTH CHECK."

          echo ""
          echo "Green logs:"
          docker logs "$GREEN_CONTAINER" || true

          exit 1


      # ======================================================
      # 10. Prepare GREEN Nginx Configuration
      # ======================================================
      - name: Prepare Green Nginx Configuration
        run: |

          echo "====================================="
          echo "PREPARING GREEN NGINX CONFIG"
          echo "====================================="

          cp /tmp/nginx-blue.conf /tmp/nginx-green.conf

          sed -i \
            's/devops-java-deploy:8080/devops-java-green:8080/' \
            /tmp/nginx-green.conf

          echo ""
          echo "Green configuration:"
          cat /tmp/nginx-green.conf


      # ======================================================
      # 11. Switch Nginx to GREEN
      # ======================================================
      - name: Switch Traffic to Green
        run: |

          echo "====================================="
          echo "SWITCHING TRAFFIC"
          echo "BLUE -> GREEN"
          echo "====================================="

          cp /tmp/nginx-green.conf nginx/nginx.conf

          echo ""
          echo "Current host configuration:"
          cat nginx/nginx.conf

          echo ""
          echo "Validating Nginx..."

          docker exec "$NGINX_CONTAINER" nginx -t

          echo ""
          echo "Reloading Nginx..."

          docker exec "$NGINX_CONTAINER" nginx -s reload

          echo ""
          echo "Nginx switched to GREEN."


      # ======================================================
      # 12. VERIFY NGINX IS REALLY USING GREEN
      # ======================================================
      - name: Verify Nginx Green Routing
        run: |

          echo "====================================="
          echo "VERIFYING NGINX GREEN ROUTING"
          echo "====================================="

          docker exec "$NGINX_CONTAINER" \
            nginx -T 2>&1 | grep "devops-java-green:8080"

          if ! docker exec "$NGINX_CONTAINER" \
            nginx -T 2>&1 | grep -q "devops-java-green:8080"; then

            echo "ERROR: Nginx is NOT pointing to Green."

            exit 1
          fi

          echo "Nginx is correctly pointing to GREEN."


      # ======================================================
      # 13. VERIFY PRODUCTION TRAFFIC
      # ======================================================
      - name: Verify Production Traffic
        run: |

          echo "====================================="
          echo "VERIFYING PRODUCTION TRAFFIC"
          echo "====================================="

          sleep 2

          curl -fsS \
            "http://localhost:$PRODUCTION_PORT/devops-lab/" \
            > /tmp/production.html

          cat /tmp/production.html

          echo ""
          echo "Production traffic is reaching GREEN."


      # ======================================================
      # 14. INTENTIONAL FAILURE
      #
      # TEMPORARY ROLLBACK TEST
      #
      # Remove/comment this step when real deployment
      # should succeed.
      # ======================================================
      - name: Simulate Post Deployment Failure
        run: |

          echo "====================================="
          echo "SIMULATING PRODUCTION FAILURE"
          echo "====================================="

          echo ""
          echo "GREEN is currently serving production traffic."

          echo ""
          echo "Intentionally failing deployment..."

          exit 1


      # ======================================================
      # 15. DEPLOYMENT SUCCESS
      #
      # This will NOT execute during rollback test because
      # previous step intentionally fails.
      # ======================================================
      - name: Deployment Successful
        if: success()
        run: |

          echo "====================================="
          echo "BLUE/GREEN DEPLOYMENT SUCCESSFUL"
          echo "====================================="

          echo "Image: $IMAGE_NAME:$IMAGE_TAG"
          echo "Active environment: GREEN"
          echo "Green container: $GREEN_CONTAINER"


      # ======================================================
      # 16. AUTOMATIC ROLLBACK TO BLUE
      # ======================================================
      - name: Automatic Rollback Traffic to Blue
        if: failure()
        run: |

          echo "====================================="
          echo "DEPLOYMENT FAILED"
          echo "ROLLING BACK TO BLUE"
          echo "====================================="

          echo ""
          echo "Green container logs:"
          docker logs "$GREEN_CONTAINER" 2>/dev/null || true


          # --------------------------------------------------
          # Restore BLUE configuration
          # --------------------------------------------------
          echo ""
          echo "Restoring BLUE Nginx configuration..."

          cp /tmp/nginx-blue.conf nginx/nginx.conf

          echo ""
          echo "Restored configuration:"
          cat nginx/nginx.conf


          # --------------------------------------------------
          # Validate Nginx
          # --------------------------------------------------
          echo ""
          echo "Validating Nginx configuration..."

          docker exec "$NGINX_CONTAINER" nginx -t


          # --------------------------------------------------
          # Reload Nginx
          # --------------------------------------------------
          echo ""
          echo "Reloading Nginx..."

          docker exec "$NGINX_CONTAINER" nginx -s reload


          # --------------------------------------------------
          # Verify Nginx is actually pointing to BLUE
          # --------------------------------------------------
          echo ""
          echo "====================================="
          echo "VERIFYING NGINX BLUE ROUTING"
          echo "====================================="

          docker exec "$NGINX_CONTAINER" \
            nginx -T 2>&1 | grep "devops-java-deploy:8080"

          if ! docker exec "$NGINX_CONTAINER" \
            nginx -T 2>&1 | grep -q "devops-java-deploy:8080"; then

            echo "ERROR: Nginx is NOT pointing to BLUE!"

            exit 1
          fi

          echo ""
          echo "Nginx is correctly pointing to BLUE."


          # --------------------------------------------------
          # Verify Production Traffic
          # --------------------------------------------------
          echo ""
          echo "====================================="
          echo "VERIFYING BLUE PRODUCTION TRAFFIC"
          echo "====================================="

          sleep 2

          curl -fsS \
            "http://localhost:$PRODUCTION_PORT/devops-lab/" \
            > /tmp/rollback.html

          echo ""
          echo "Production response:"
          cat /tmp/rollback.html


          # --------------------------------------------------
          # Verify V5 specifically
          #
          # Change V5 to your actual Blue version if needed.
          # --------------------------------------------------
          echo ""
          echo "Checking Blue application version..."

          if ! grep -q "Application Versioning:V5" /tmp/rollback.html; then

            echo ""
            echo "ERROR: Production is NOT serving Blue V5!"

            exit 1
          fi

          echo ""
          echo "====================================="
          echo "ROLLBACK SUCCESSFUL"
          echo "BLUE V5 IS ACTIVE"
          echo "====================================="


      # ======================================================
      # 17. FINAL CONTAINER STATE
      # ======================================================
      - name: Show Final State
        if: always()
        run: |

          echo "====================================="
          echo "FINAL CONTAINER STATE"
          echo "====================================="

          docker ps \
            --filter "name=$BLUE_CONTAINER" \
            --filter "name=$GREEN_CONTAINER" \
            --filter "name=$NGINX_CONTAINER" \
            --format "table {{.Names}}\t{{.Image}}\t{{.Ports}}"

          echo ""
          echo "====================================="
          echo "FINAL NGINX CONFIGURATION"
          echo "====================================="

          docker exec "$NGINX_CONTAINER" nginx -T 2>&1 || true
